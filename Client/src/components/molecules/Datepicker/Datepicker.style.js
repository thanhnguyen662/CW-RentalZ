import { StyleSheet } from 'react-native';

export default StyleSheet.create({
   button: {
      alignItems: 'flex-start',
      borderColor: '#e5e5e5',
      borderWidth: 1,
      padding: 8,
      borderRadius: 4,
      height: 42,
   },
   dateValue: {
      fontSize: 13,
      color: '#1f2937',
   },
   buttonError: {
      alignItems: 'flex-start',
      borderColor: 'red',
      borderWidth: 1,
      padding: 8,
      borderRadius: 4,
      height: 42,
   },
});
